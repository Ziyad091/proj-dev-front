export class Caracteristique {

    nom: string;
    descriptifs: string[];

    constructor(nom: string, descriptifs: string[]) {
        this.nom = nom;
        this.descriptifs = descriptifs;
    }
}
